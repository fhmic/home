{
  "name": "fhmic-home",
  "version": "1.0.0",
  "private": true,
  "description": "Source + build pipeline for fhmic.github.io/home. Edit files in src/, run `npm run build` to produce the concealed docs/ output that GitHub Pages actually serves.",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "terser": "^5.31.0",
    "clean-css": "^5.3.3"
  }
}
